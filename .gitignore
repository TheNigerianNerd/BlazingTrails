# Exclude standard .NET build output
bin/
obj/

# Exclude published output
# This assumes you publish to a 'publish' directory
# You may also want to exclude wwwroot/_framework if you find it's being tracked
# and causing issues, though a good build process shouldn't commit it.
publish/

# Exclude Blazor-specific artifacts
# These are generated and should not be tracked
wwwroot/service-worker-assets.js
wwwroot/_framework/

# Ignore files produced by Visual Studio
.vs/
*.user
*.suo

# Ignore local run settings
appsettings.Development.json